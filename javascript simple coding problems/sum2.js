const numbers = [44, 23, 34, 32, 54, 5, 78];
for (let i = 0; i < numbers.length; i++) {
    const element = numbers[i]; //i er maan baar baar change hoye bivinno index e niye geche //ekhane ektu debug krbo
    console.log(element);
}
//watch e i er maan diye dilam
